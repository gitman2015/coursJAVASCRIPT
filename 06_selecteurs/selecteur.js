//avec cet évenement windows.onload toute la page est chargée et cela permet de mettre ke script dans head sans async ou defer

window.onload=function(){
    var select1=titreN1;
    // console.info(select1);
}

